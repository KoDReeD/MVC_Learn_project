@using Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<WebStore.Models.ApplicationType>;

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Applicarion Type List</h2>
        </div>
        <div class="col-6" style="text-align: end">
            <a class="btn btn-primary"
               asp-controller="ApplicationType" asp-action="AddEdit">
                Create Application Type
            </a>
        </div>
    </div>

    <br/>

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped"
               style="width: 100%">
            <thead>
            <tr>
                <th>Title</th>
                <th></th>
            </tr>
            </thead>

            <tbody>
            @foreach (var appType in Model)
            {
                <tr>
                    <td width="50%">@appType.Title</td>
                    <td width="20%">
                        <div class="text-center">
                            <div class="w-50 btn-group" role="group">
                                <a asp-action="AddEdit" asp-route-Id="@appType.Id" class="btn btn-primary me-2">
                                    <i class="fa-solid fa-pen-to-square"></i>
                                </a>
                                <a asp-action="Delete" asp-route-Id="@appType.Id" class="btn btn-danger">
                                    <i class="fa-solid fa-trash-can"></i>
                                </a>
                            </div>
                        </div>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    }
    else
    {
        <h2>No category exists</h2>
    }
</div>